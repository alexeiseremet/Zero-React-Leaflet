// import {types} from './actions';

// initial state
const initialState = {};

const Markers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default Markers
