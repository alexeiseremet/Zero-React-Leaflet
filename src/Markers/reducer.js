// import {types} from './actions';

// initial state
const initialState = {};

const App = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default App
